name: Deploy to apk file

on:
  workflow_dispatch:
    inputs:
      version:
        description: eg. 1.0.0
        required: true

      description:
        description: What's new?
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./src
    permissions:
      contents: write
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK 25
        uses: actions/setup-java@v3
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - run: |
          sed -i 's/^version: .*/version: ${{ github.event.inputs.version }}/' pubspec.yaml

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.41.6

      - name: Decode keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > android/app/torri-release.jks
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}

      - run: flutter build apk --release
        env:
          KEYSTORE_PATH: torri-release.jks
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}

      - run: mv build/app/outputs/apk/release/app-release.apk Torri-${{ github.event.inputs.version }}.apk

      - name: Create tag
        run: |
          git tag -f v${{ github.event.inputs.version }}
          git push --force origin v${{ github.event.inputs.version }}

      - name: Create Gitea Release
        uses: akkuman/gitea-release-action@v1
        with:
          name: Release ${{ github.event.inputs.version }}
          tag_name: v${{ github.event.inputs.version }}
          body: ${{ github.event.inputs.description }}
          draft: false
          prerelease: false
          files: |
            src/Torri-${{ github.event.inputs.version }}.apk
